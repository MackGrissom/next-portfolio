const colors = require('tailwindcss/colors')

const theme = {
  // Primary color
  alpha: colors.indigo,
  // Secondary color
  beta: colors.gray,
  // Accent color
  accent: colors.indigo,
  // Neutral color
  omega: colors.zinc,
}

module.exports = theme
