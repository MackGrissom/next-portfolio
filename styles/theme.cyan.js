const colors = require('tailwindcss/colors')

const theme = {
  // Primary color
  alpha: colors.gray,
  // Secondary color
  beta: colors.indigo,
  // Accent color
  accent: colors.red,
  // Neutral color
  omega: colors.zinc,
}

module.exports = theme
